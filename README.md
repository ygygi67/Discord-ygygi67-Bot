# 🌌 ygygi67 Bot — Distributed Discord Super Bot

> บอท Discord สายครบเครื่องที่รวม **AI + Music + Intercom + Moderation + Voice Tools**  
> ออกแบบให้รันได้ทั้งแบบเครื่องเดียว และแบบกระจายงานหลาย worker/shard สำหรับเซิร์ฟเวอร์ขนาดใหญ่ 🚀

---

## 🎯 Project Vision

`ygygi67 Bot` ถูกสร้างมาเพื่อให้ “เซิร์ฟเวอร์เดียวจบทุกงาน”  
ไม่ต้องสลับหลายบอทให้วุ่นวาย ทั้งงานคุย, งานดูแล, งานเพลง, งานเชื่อมข้ามเซิร์ฟ, และงาน automation พื้นฐาน

สิ่งที่โปรเจกต์นี้เน้น:

- ⚡ **เร็วและเสถียร** ด้วยโครงสร้าง Distributed
- 🧩 **ขยายต่อได้** ด้วย Cog-based architecture
- 🛡️ **ปลอดภัยขึ้น** ด้วย Intercom Security Control Panel
- 🎙️ **ใช้งานจริงได้** ด้วยระบบ Voice/TTS/Transcription

---

## ✨ Feature Showcase

### 🤖 AI Assistant
- ห้อง AI ส่วนตัวและโซน AI
- จดจำบริบท/สรุปบทสนทนา
- โหมดบุคลิก AI หลายรูปแบบ

### 🎵 Music & Voice
- เล่นเพลงและจัดคิว
- สั่ง TTS ด้วย `/พูดตาม`
- ถอดเสียงจากข้อความด้วย `/ถอดเสียงข้อความ`
  - รองรับลิงก์ข้อความ Discord
  - รองรับ Message ID + ระบุช่อง
  - เลือกได้ว่าจะส่งไฟล์เสียงต้นฉบับกลับมาด้วยหรือไม่

### 🌐 Intercom Cross-Server
- Broadcast ข้ามเซิร์ฟเวอร์
- ส่งแบบเจาะจงเซิร์ฟ (`/intercom_private`)
- ส่ง DM ข้ามเซิร์ฟ (`/intercom_dm`)
- รองรับ sync แก้ไขข้อความต้นทางไปปลายทาง
- แสดง `UID / GID / MID` ในข้อความปลายทาง

### 🎛️ Intercom Security Control Panel
- กันสแปม
- กรองคำไม่เหมาะสม (ค่าเริ่มต้น + เพิ่มคำเอง)
- กรองลิงก์ด้วย allowlist
- ตั้งช่อง log เหตุการณ์ความปลอดภัย
- ลบข้อความต้นทาง+ปลายทางด้วย `/intercom_purge`

### 🛠️ Server Management
- ชุดคำสั่ง admin/moderation
- ระบบห้อง private chat
- ระบบ status/stats/notification/logging
- ระบบลบข้อความขั้นสูงแบบกรองตามผู้ใช้/เว็บฮุก/แอป
- ระบบ TempVoice สร้างห้องเสียงอัตโนมัติ
- ระบบ ServerStats แก้เลขจำนวนผู้ใช้/บอทอัตโนมัติ

---

## 🆕 อัปเดตล่าสุด

- ✅ ปรับ `/โหลดคลิป` ให้กันค้างจากลิงก์ YouTube ที่พ่วง playlist และจัดการข้อความ error ให้อ่านง่ายขึ้น
- ✅ เพิ่ม fallback ปุ่มสลับเป็น MP3 เมื่อไฟล์ใหญ่เกิน และแจ้งเตือนกรณีเกิน 200 MB
- ✅ เพิ่ม `/แปลงไฟล์` แบบหาเส้นทางแปลงอัตโนมัติ (conversion path) สำหรับหลายรูปแบบ
- ✅ อัปเกรด `/ล้างข้อความ`:
  - ลบได้ทั้ง **ห้องปัจจุบัน** หรือ **ทุกห้องในเซิร์ฟเวอร์**
  - กรองด้วย `ผู้ใช้`, `User ID`, `Webhook ID`, `Application ID`, เวลา, บอท/เว็บฮุก/แอป
  - มีปุ่ม `⏸️ หยุดชั่วคราว` และ `🛑 หยุดการทำงาน`
  - ลดปัญหา 429 ด้วยการลบแบบ **Bulk** (ข้อความใหม่) + ลบทีละข้อความแบบหน่วงเวลา (ข้อความเก่า)
- ✅ เพิ่ม `/ตรวจสอบข้อความ` สำหรับเช็กต้นทางของข้อความ (author/webhook/application)
- ✅ เพิ่ม `/ค้นหาข้อความ` สำหรับค้นประวัติว่าใครเคยพิมพ์อะไร (เลือกผู้ใช้หรือใส่ ID ได้)
- ✅ เพิ่มระบบ **TempVoice** + **ServerStats Dynamic Channels**
  - ตั้งค่าให้บอทสร้างช่องให้เอง หรือเลือกช่องที่มีอยู่ก็ได้
  - ServerStats จะอัปเดตเฉพาะตัวเลขท้ายชื่อช่อง และแก้ข้อความหน้าเลขได้ตลอด
  - TempVoice รองรับชื่อห้องแบบ template เช่น `{display}` `{user}` `{num}` และย้ายคนเข้าห้องใหม่อัตโนมัติ
- ✅ เพิ่ม **Roblox Presence Board (Panel)**
  - คำสั่ง `/สถานะกระดานroblox` รองรับการใส่หลายชื่อ/ID และดึงเพื่อนจากบัญชี Roblox
  - รองรับสูงสุด **1,000 คน** และแสดงผลแบบตารางแบ่งหน้า
  - มีปุ่มควบคุม: รีเฟรช, เรียงตามชื่อ/ออนไลน์ล่าสุด/เวลาเล่น, เพิ่มรายชื่อ, ดึงเพื่อน, หยุด/เริ่มอัปเดตอัตโนมัติ
  - เพิ่มปุ่มเลื่อนหน้าแบบเร็ว: ถัดไป/ย้อนกลับ, กระโดด `±5` หน้า, กระโดด `±10` หน้า, ไปหน้าสุดท้าย
- ✅ เพิ่ม `/โปรไฟล์กิจกรรมroblox`
  - สรุป `ออนไลน์ล่าสุด`, ชั่วโมงเล่นย้อนหลัง `1 วัน / 7 วัน / 30 วัน`
  - แสดงเวลาออนไลน์ล่าสุดทั้งแบบเวลาเต็มและแบบสัมพันธ์ (เช่น `3 นาทีที่แล้ว`)
  - แสดงช่วงเวลาที่เล่นบ่อย และเวลาที่เข้าเกมล่าสุด
  - ถ้ายังไม่มีข้อมูลในระบบ ระบบจะเริ่มติดตามและเก็บประวัติให้อัตโนมัติทันที
  - แสดงรูปโปรไฟล์ Roblox ใน Embed
- ✅ แก้บัค Slash Command Roblox บางครั้งเด้ง `Unknown interaction (10062)`
  - เพิ่ม safe defer ในคำสั่ง `/โปรไฟล์กิจกรรมroblox` เพื่อกันเคส interaction หมดอายุ
  - เมื่อ defer ไม่สำเร็จ ระบบจะ log เป็น warning และจบคำสั่งอย่างปลอดภัย (ไม่โยน traceback)
- ✅ แก้บัค `ServerLogger` ในเหตุการณ์เสียง (`on_voice_state_update`)
  - กันเคส `aiohttp.client_exceptions.ClientConnectionError: Connector is closed` ระหว่างอ่าน Audit Log
  - ปรับให้ fallback เป็น warning log และไม่ทำให้ event handler ล่ม
- ✅ แก้บัค **Roblox Presence Board หลังรีบูต**
  - บอร์ดที่บอทส่งไว้จะรีไฮเดรตปุ่มกลับมาอัตโนมัติ (ไม่เหลือแค่ embed)
  - เพิ่มระบบบันทึกสถานะบอร์ด/กิจกรรมลงไฟล์ข้อมูลเพื่อให้ทำงานต่อหลังรีสตาร์ท
- ✅ ปรับเสถียรภาพ **Presence Task**
  - เปลี่ยนเป็น batch query แบบ retry/backoff + split chunk เพื่อลดปัญหา `429`
  - จัดการเคส restart/shutdown ให้ไม่เกิด traceback รบกวนคอนโซล
- ✅ ปรับ **Console Control** ให้ใช้ตัวย่อได้และใช้ง่ายขึ้น
  - ตัวอย่าง: `s`=`stop`, `r`=`restart`, `d`=`disable`, `e`=`enable`, `sy`=`sync`, `gs`=`guildsync`
  - เมื่อพิมพ์ `disable/enable` ไม่ครบ จะมีข้อความบอก usage และตัวอย่างคำสั่งต่อทันที
  - เพิ่ม log สถานะ Roblox Board ในคอนโซลระหว่างรีเฟรช/อัปเดตอัตโนมัติ
  - เพิ่ม log เริ่ม/จบ/error ของ Slash Commands ในคอนโซลเพื่อตรวจว่า “บอทยังคำนวณอยู่”
- ✅ เพิ่มระบบ **AI Karaoke (Vocal Separation)** ในระบบเพลง:
  - ใช้คำสั่ง `/เล่น` แล้วเลือก `mode: คาราโอเกะ` บอทจะใช้ AI ตัดเสียงร้องออกให้อัตโนมัติ
  - ใช้โมเดลระดับสูง `UVR-MDX-NET-Voc_FT` เพื่อคุณภาพดนตรี (Instrumental) ที่ดีที่สุด
  - มีข้อความแจ้งสถานะการประมวลผล (Preparing > AI Separation > Completed)
- ✅ ปรับปรุงระบบ **Vocal Separator (`/แยกเสียงร้อง`)**:
  - เพิ่ม **Progress Bar** และ Animation แสดงสถานะการถอดเสียงเพื่อให้ไม่ดูเหมือนบอทค้าง
  - ตั้งชื่อไฟล์ผลลัพธ์ตามชื่อเพลงจริงโดยอัตโนมัติ (เช่น `ชื่อเพลง (Vocals).mp3`)
  - แก้ไขบัค Interaction Timeout และรองรับลิงก์ Playlist (โดยโหลดเฉพาะคลิปที่เลือก)
- ✅ ปรับปรุงระบบ **Roblox Followers**:
  - เพิ่มระบบแจ้งเตือนแบบ **State Persistence** ป้องกันบอทแจ้งเตือนซ้ำซ้อนหลังจากการรีบูต
  - เพิ่ม Cooldown แจ้งเตือน 45 วินาทีต่อคน เพื่อลดความวุ่นวายจาก API Flickering
- ✅ ย้ายระบบ **Youtube Spy** เข้ามาเป็น Cog สมบูรณ์แบบ:
  - รองรับการสแกนความเคลื่อนไหว (Subscriber/Views/New Video) ทุก 10 นาที
  - ควบคุมผ่านคำสั่ง `/youtube-spy` (Scan / Status / Reset DB)
- ✅ เพิ่มระบบ **Persistent Task Queue (SQLite Backed)**:
  - งานที่ประมวลผลนาน (เช่น แยกเสียงร้อง) จะถูกบันทึกลงฐานข้อมูลเพื่อให้ทำงานต่อได้แม้บอทรีสตาร์ท
  - ระบบ **Task Recovery**: กู้คืนงานที่ค้าง (Processing) ให้กลับมาเริ่มใหม่โดยอัตโนมัติเมื่อบอทออนไลน์
  - ระบบ **Parallel Processing**: รองรับการประมวลผลงานจากคิวพร้อมกัน และเช็คคิวรวดเร็วทุก 1 วินาที
- ✅ ระบบ **Roblox Presence Tracking (ความเสถียรสูง)**:
  - เพิ่มระบบ **Primary Instance Lock**: ป้องกันบอทส่งแจ้งเตือนสถานะซ้ำซ้อนเมื่อรันหลาย Shard หรือหลายเครื่อง
  - เพิ่มระบบ **Just-In-Time State Check**: ตรวจสอบสถานะจริงก่อนส่งข้อความเสี้ยววินาที เพื่อความนิ่ง 100%
  - ระบบล้างข้อมูลซ้ำ (Deduplication) อัตโนมัติในฐานข้อมูลผู้ใช้ที่ติดตาม
- ✅ เสริมแกร่งระบบ **AI Vocal Separator** & **Smart Features**:
  - ✨ นำคำสั่ง `/เช็คคิวแยกเสียง` ออกเพื่อความสะอาดของระบบ เนื่องจากสถานะถูกแสดงใน Progress Bar อัตโนมัติแล้ว
  - 🔍 **New Feature**: เพิ่มคำสั่ง `/หาเพลง` (Smart Music Search) ใช้ AI วิเคราะห์หาชื่อเพลง, ศิลปิน และแนวเพลงจากไฟล์เสียง/วิดีโอ โดยตอนนี้รองรับการสแกนผ่าน **ลิงก์ (YouTube/TikTok/Facebook)** สะดวกโดยไม่ต้องโหลดไฟล์เอง
  - 🧠 **AI Instrumental Fallback**: หากค้นหาเพลงไม่เจอ(อาจมีเสียงพูดทับ) ระบบจะใช้ AI แยกดนตรีล้วน (Instrumental) ออกมาสแกนใหม่อัตโนมัติ
  - 🛡️ **TikTok Restriction Handling**: เพิ่มระบบตรวจจับคลิป TikTok ที่โดนจำกัดอายุ (Age-restricted) เพื่อแจ้งเตือนให้ผู้ใช้งานอัปโหลดไฟล์แทนการใช้ลิงก์
  - แก้ไขปัญหา **Payload Too Large (413)**: ระบบจะส่งไฟล์ผลลัพธ์ทีละไฟล์ และอัปโหลดขึ้น Cloud (Catbox) อัตโนมัติหากไฟล์ใหญ่เกิน 10MB
  - เพิ่มระบบ **Dynamic Progress Bar & ETC**: ระบบบันทึกสถิติและคำนวณเวลาประมวลผลที่เหลือล่วงหน้าให้ผู้ใช้งานแบบ Real-time
  - แก้ไข Bug **TypeError** ในระบบคิว และปัญหาไฟล์ I/O Error
  - 🛠️ **Bug Fix**: แก้ไขปัญหาระบบสร้างโฟลเดอร์ `tmp` ที่หน้าไดรฟ์ โดยกำหนดให้บันทึกโมเดลไว้ในโฟลเดอร์ `data/models/separator/` อย่างเป็นระเบียบ และปรับปรุงการแสดง Error Log ให้แสดงสาเหตุที่แท้จริงเมื่อระบบล่ม
  - ปรับปรุงความเร็วในการดึงข้อมูลจาก YouTube และความแม่นยำของ Model ID
  - 🎨 **New Feature: AI Media Generator**: เพิ่มคำสั่ง `/สร้างภาพ` และ `/สร้างวิดีโอ` รองรับการรันผ่าน Stable Diffusion (Local) พร้อมระบบ **Cloud Fallback** (Pollinations AI) เพื่อให้ใช้งานได้ทันทีแม้ไม่มีการ์ดจอแรงๆ ในเครื่อง
  - 🎙️ **TTS persistence (พูดตาม)**: ระบบ `/พูดตาม` รองรับการทำงานแบบ **Auto-Resume** (คุยค้างไว้แล้วรีบอท บอทจะมุดกลับมาพูดต่อให้เอง) และสามารถสั่งให้ส่งแค่ไฟล์เสียงโดยไม่ต้องเข้าห้องได้แล้ว
  - 🏃 **Smart Voice Follow**: ปรับปรุงระบบบอทเดินตามคนไปช่องอื่นให้เป็นแบบ "ถามความสมัครใจ" ผ่านปุ่มกดแทนการบินตามอัตโนมัติ เพื่อไม่ให้รบกวนผู้ใช้งาน
  - 🛡️ **Safe Restart (sr)**: เพิ่มระบบรีสตาร์ทบอทแบบปลอดภัยผ่าน Console (`sr`) โดยบอทจะหยุดรับคำสั่งใหม่และรอจนกว่างานประมวลผล (แยกเสียง/พูดตาม) ทั้งหมดจะเสร็จสิ้นก่อนจึงจะทำการรีเซ็ตตัวเอง
  - 🔌 **Stable Connection**: เพิ่มระบบ Retry อัตโนมัติเมื่อเกิด `ClientConnectionResetError` ระหว่างพยายาม Reconnect เข้าห้องเสียง เพื่อความนิ่ง 100%
  - 🤖 **Auto-Sync Commands**: ปรับปรุงระบบซิงค์คำสั่ง Slash Command ให้ทำงานอัตโนมัติทันทีที่บอทเข้าเซิร์ฟเวอร์ใหม่ หมดปัญหา "คำสั่งไม่ขึ้น" หลังเชิญบอท
- ✅ ระบบ **Graceful Subprocess Shutdown**:
  - เพิ่มการติดตาม Process (yt-dlp/AI) เพื่อสั่งปิดงานให้สะอาดเมื่อบอทหยุดทำงาน 
  - ป้องกันปัญหา `Event loop is closed` และลดการกินทรัพยากรค้างคาวบน Windows
- ✅ แก้ไขจุดบกพร่อง (Bug Fixes):
  - ปรับชื่อเรียก Model ID ให้ตรงตามมาตรฐาน `audio-separator v0.44+` 
  - แก้ไขปัญหาโมเดลหาไม่พบ (ValueError: Model file not found)
  - เพิ่มระบบ Safe Send/Edit ครอบคลุมทุกจุดเพื่อความนิ่ง 100%
- ✅ ปรับปรุง **ความเสถียรของ Cog Loader**:
  - เพิ่มระบบกรองโฟลเดอร์อัตโนมัติ ข้ามโฟลเดอร์ `.venv`, `node_modules` และโฟลเดอร์ระบบอื่นๆ อย่างเด็ดขาด
  - เพิ่มการตรวจสอบฟังก์ชัน `setup` ก่อนโหลด ป้องกัน Error กรณีที่มีไฟล์จำพวก Utility หรือ Script ช่วยงานอยู่ในโฟลเดอร์ Cogs
- ✅ แก้ไขปัญหา **Youtube Spy**:
  - สยบ Error Log เกี่ยวกับ Channel Tabs ที่หายไปหรือเป็นส่วนตัว (Missing tabs log spam)
  - ปรับระบบให้ทำงานได้นิ่งขึ้นในกรณีที่ YouTube เปลี่ยนโครงสร้างหน้าเว็บ

---

## 🧭 Command Map (หมวดหลัก)

### Setup
- `/setup_server`
- `/intercom_setup`

### Intercom
- `/intercom_panel`
- `/intercom_security`
- `/intercom_private`
- `/intercom_dm`
- `/intercom_purge`

### Voice
- `/พูดตาม`
- `/ถอดเสียงข้อความ`
- `/โหลดคลิป`
- `/แปลงไฟล์`
- `/แปลงไฟล์_รองรับ`

### Roblox
- `/เช็คโปรไฟล์roblox`
- `/โปรไฟล์กิจกรรมroblox`
- `/ติดตามสถานะroblox`
- `/ยกเลิกติดตามroblox`
- `/รายการติดตามroblox`
- `/สถานะกระดานroblox`
- `/เครือข่ายroblox`

### Admin / Security
- `/ล้างข้อความ`
- `/ตรวจสอบข้อความ`
- `/ค้นหาข้อความ`

### TempVoice / ServerStats
- `/ตั้งค่า_tempvoice`
- `/ตั้งชื่อ_tempvoice`
- `/เปลี่ยนชื่อห้อง_tempvoice`
- `/ตั้งค่า_serverstats`
- `/ตั้งชื่อ_serverstats`
- `/youtube-spy`

---

## ⚙️ คำสั่งการทำงาน (พร้อมตัวอย่าง)

### `/โหลดคลิป url:<ลิงก์> mode:<mp4|mp3>`
- หน้าที่: ดาวน์โหลดวิดีโอ/เสียงจากลิงก์ YouTube, TikTok และแหล่งที่รองรับ
- จุดเด่น: แสดงสถานะดาวน์โหลดแบบอัปเดตข้อความ, มี fallback กดปุ่มแปลงเป็น MP3 เมื่อไฟล์ใหญ่เกิน
- ตัวอย่างข้อความบอท:
  - `⏳ กำลังตรวจสอบและเตรียมดาวน์โหลดจาก ...`
  - `📊 52.3% | 12.40/23.70 MB`
  - `⚠️ ไฟล์ใหญ่เกิน 200 MB ... กดปุ่มเพื่อสลับเป็น MP3`
  - `✅ ดาวน์โหลดสำเร็จ`

### `/แปลงไฟล์ file:<ไฟล์แนบ> target_format:<ปลายทาง>`
- หน้าที่: แปลงไฟล์แบบอัตโนมัติด้วยเส้นทางแปลง (conversion path)
- รองรับ: ข้อความ ↔ ข้อความ, สื่อ ↔ สื่อ (รูป/เสียง/วิดีโอ)
- ตัวอย่างข้อความบอท:
  - `🔄 กำลังแปลง (1/2) MP4 ➜ MP3 ...`
  - `✅ แปลงเสร็จแล้ว MP4 ➜ MP3`
  - `❌ ยังไม่รองรับเส้นทางแปลงไฟล์นี้`

### `/แปลงไฟล์_รองรับ`
- หน้าที่: แสดงรายการหมวดไฟล์ที่ระบบรองรับทั้งหมดอย่างรวดเร็ว

### `/ถอดเสียงข้อความ`
- หน้าที่: ถอดเสียงจากข้อความที่มีไฟล์เสียง/ลิงก์ข้อความ
- รองรับ: ใส่ลิงก์ข้อความ Discord หรือ Message ID + ช่อง

### `/พูดตาม`
- หน้าที่: ให้บอทพูดข้อความในห้องเสียงด้วย TTS
- เหมาะกับ: ประกาศ, แจ้งเตือน, โหมดผู้ช่วยเสียง

### `/ตั้งค่า_serverstats`
- โหมดที่รองรับ:
  - ให้บอทสร้างช่องให้ (สร้างหมวด + ช่องผู้ใช้ + ช่องบอทอัตโนมัติ)
  - ใช้ช่องที่มีอยู่ (เลือกเองได้ว่าจะตั้งเฉพาะช่องผู้ใช้, เฉพาะช่องบอท, หรือทั้งสองช่อง)
- การทำงาน:
  - บอทจะอัปเดตตัวเลขตามสมาชิกจริง (ผู้ใช้/บอท)
  - เปลี่ยนได้เฉพาะตัวเลขท้ายชื่อ โดยข้อความหน้าตัวเลขตั้งเองได้
  - ช่องนับสถิติจะถูกตั้งค่าไม่ให้สมาชิกทั่วไปเข้าใช้งาน (connect=False)
  - ถ้าไม่ใส่โหมด จะเปิดเป็น **Panel Interface**

### `/ตั้งชื่อ_serverstats`
- ใช้แก้ prefix ของชื่อช่อง เช่น `👥 ผู้ใช้:` หรือ `🤖 บอท:`
- รองรับ `{count}` ใน template เช่น `👥 ออนไลน์ตอนนี้ {count}`
- ถ้าเรียกคำสั่งโดยไม่ใส่พารามิเตอร์ จะเปิด **Panel Interface** (ปุ่ม/Modal)

### `/ตั้งค่า_tempvoice`
- โหมดที่รองรับ:
  - ให้บอทสร้างหมวดและล๊อบบี้ให้เอง
  - ใช้หมวด/ล๊อบบี้ที่มีอยู่
- การทำงาน:
  - เมื่อมีคนเข้าห้องล๊อบบี้ บอทจะสร้างห้องใหม่ตาม template แล้ว **ย้ายผู้ใช้เข้าห้องนั้นทันที**
  - ตั้งชื่อได้แบบ dynamic เช่น `🔊 {display} #{num}` / `🏠 ห้องของ {user}`
  - ห้องชั่วคราวจะถูกลบเมื่อว่าง
  - ถ้าเรียกคำสั่งโดยไม่ใส่โหมด จะเปิด **Panel Interface** สำหรับตั้งค่าแบบกดปุ่ม

---

## 🗣️ ประโยคตอบกลับของบอท (ใช้งานจริง)

- `⏳ กำลังเริ่มดาวน์โหลดโหมด MP4 ...`
- `🔄 ดาวน์โหลดเสร็จแล้ว กำลังประมวลผลไฟล์ MP3...`
- `☁️ ไฟล์ใหญ่เกิน limit กำลังอัพโหลดขึ้น catbox.moe...`
- `❌ คลิปนี้ไม่สามารถเข้าถึงได้ (อาจโดนปิด/จำกัดประเทศ/ต้องล็อกอิน)`
- `✅ ส่งไฟล์เรียบร้อย`
- `❌ เกิดข้อผิดพลาดในการโหลด: ...`

---

## 🧠 Runtime Modes

| Mode | เหมาะกับ | ตัวอย่างคำสั่ง |
|------|----------|----------------|
| `standalone` | เครื่องเดียว / เริ่มต้นเร็ว | `python bot.py` |
| `master` | คุม shard/cluster | `BOT_MODE=master python bot.py` |
| `worker` | รับงานหนักแยก process | `python worker_node.py` |

---

## 🔢 ทำไมบางทีเห็นเกิน 100 คำสั่ง

Discord จำกัดจำนวน **Slash Commands ต่อ scope** ไม่ใช่ทั้งระบบรวมกัน:

- `Global Commands` จำกัดประมาณ **100**
- `Guild Commands` (ต่อ 1 เซิร์ฟเวอร์) จำกัดประมาณ **100**

ดังนั้นใน log อาจเห็นตัวเลขรวมเกิน 100 ได้ เช่น `103` เพราะระบบรวมรายการจาก 2 scope มารายงาน:

- คำสั่ง global
- คำสั่ง guild-only (เช่น AI Panel / Voice / Utility บางส่วน)

> สรุป: `103` ใน log **ไม่แปลว่าผิดเสมอไป** ถ้ามาจากการรวมหลาย scope  
> แต่ถ้าไป `sync` scope เดียวแล้วเกิน 100 จะเจอ `CommandLimitReached`

### แนวทางที่ใช้ในโปรเจกต์นี้

- แยกคำสั่งบางส่วนให้เป็น **guild-scoped** เพื่อลดการชนเพดาน global
- Sync global และ guild แยกกัน (ไม่ copy ซ้ำทั้งหมดลง guild)
- รวมรายการเฉพาะตอนพิมพ์ log เพื่อให้เห็นภาพรวมคำสั่งทั้งหมด

---

## ⚡ Quick Start

### 1) Install dependencies

```bash
pip install -r requirements.txt
```

### 2) Configure environment

```bash
copy .env.example .env
```

ค่าที่ต้องมีอย่างน้อย:

- `DISCORD_TOKEN`
- `APPLICATION_ID`
- `DISCORD_GUILD_ID`

> ถ้าใช้ถอดเสียง ต้องมี `ffmpeg` ใน PATH ด้วย

### 3) Run

```bash
python bot.py
```

หรือรันแบบคลัสเตอร์:

```bash
start_cluster.bat
```

---

## 🗂️ Project Structure (อัปเดตตามโครงจริง)

```text
ygygi67 Bot/
├─ bot.py
├─ worker_node.py
├─ start.bat
├─ start_cluster.bat
├─ RestartNow.bat.bat
├─ requirements.txt
├─ .env.example
│
├─ cogs/
│  ├─ admin/
│  │  ├─ admin.py
│  │  ├─ moderation.py
│  │  ├─ roles.py
│  │  ├─ server_copier.py
│  │  └─ sync_and_manage.py
│  ├─ ai/
│  │  └─ ai_discord_bot.py
│  ├─ core/
│  │  └─ base.py
│  ├─ download_video/
│  │  └─ download_video.py
│  ├─ music/
│  │  └─ music.py
│  ├─ Roblox/
│  │  └─ Followers.py
│  ├─ utility/
│  │  ├─ bot_notifications.py
│  │  ├─ discovery.py
│  │  ├─ maintenance.py
│  │  ├─ modmail.py
│  │  ├─ server_link.py
│  │  ├─ server_logger.py
│  │  ├─ stats.py
│  │  ├─ status.py
│  │  ├─ suggestion.py
│  │  ├─ utility.py
│  │  └─ vocal_separator.py
│  ├─ YoutubeLog/
│  │  └─ youtube_super_bot.py
│  └─ voice/
│     └─ tts.py
│
├─ core/
│  ├─ command_logger.py
│  ├─ distributed_config.py
│  ├─ shard_manager.py
│  ├─ shared_queue.py
│  ├─ shared_queue_sql.py
│  ├─ sql_config.py
│  └─ storage.py
│
├─ data/
├─ docs/
│  ├─ README.md
│  └─ SETUP_GUIDE.md
├─ scripts/
├─ ffmpeg/
├─ VoiceLoggerBot/
├─ logs/            # runtime
├─ exports/         # runtime
├─ build/           # generated
└─ dist/            # generated
```

---

## 🔐 Security & Ops Notes

- อย่า commit `.env` เด็ดขาด
- แนะนำตั้ง `intercom_log_channel_id` เพื่อ audit เหตุการณ์
- โฟลเดอร์ `logs/`, `exports/`, `dist/`, `build/` เป็น output/runtime
- ก่อน deploy ควรทดสอบคำสั่งสำคัญในเซิร์ฟเวอร์ staging

---

## 📄 License

MIT

---

Made with ❤️ using `discord.py`
